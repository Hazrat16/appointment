# Copy to .env.local for local development (Next.js loads .env.local automatically).
# Base URL of the Express API (must include /api — see src/lib/api.ts).

NEXT_PUBLIC_API_URL=http://localhost:5000/api
