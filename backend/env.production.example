# Production Environment Variables
# Copy this file and rename to .env on your server
# Update all values with your production credentials

# Server Configuration
PORT=5000
NODE_ENV=production

# Database Configuration
# Replace with your production MongoDB connection string
MONGODB_URI=mongodb://localhost:27017/appointment_production
# Or use MongoDB Atlas:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/appointment?retryWrites=true&w=majority

# JWT Configuration
# Generate a strong secret key for production
# You can use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-use-crypto-random
JWT_EXPIRE=7d

# CORS Configuration
# Update with your actual domain
FRONTEND_URL=https://yourdomain.com

# Optional: Email Configuration (for future features)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Optional: File Upload Configuration
# MAX_FILE_SIZE=10485760
# UPLOAD_PATH=/var/www/appointment/uploads

